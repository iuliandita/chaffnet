[project]
name = "chaffnet-model-tools"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
model = [
    "numpy>=2.0,<3",
    "onnx>=1.17,<2",
    "onnxruntime>=1.20,<2",
    "scipy>=1.14,<2",
]

[tool.uv]
package = false
